Fix Gemini quota/failure resilience: prompt budget, memory slimming, model fallback, delay notice

Live run diagnosis: the CI failure was 429 RESOURCE_EXHAUSTED - the
prompt (24 candidates x 7000 chars + full historical memory dicts)
exceeded the free-tier 250K-token/min quota, and the 5s/10s retries
landed in the same exhausted minute. Then 503 "high demand" on
gemini-3.6-flash exhausted all attempts.

- analyze.py: GEMINI_PROMPT_CHAR_BUDGET (200K chars) caps source
  content (3000 chars/candidate); historical memory slimmed to
  id/title/category/url (~26K tokens worst case, 12x under quota);
  retries honor the API "retry in Xs" hints; automatic fallback to
  gemini-2.5-flash when the primary model keeps failing
- main.py: on crash or failed delivery, send a "report delayed"
  Telegram notice instead of silence (fixes a NameError in the
  failure handler caught by the live run)
- Verified with a real end-to-end run: 10/10 Telegram chunks
  delivered, state updated, exit 0

🤖 Generated with Codebuff
Co-Authored-By: Codebuff <noreply@codebuff.com>
